[Unit]
After=libvirtd.service
Wants=libvirtd.service

[Service]
# smb.conf binds virbr0 by name, so the bridge must already hold its address.
ExecStartPre=-/usr/bin/timeout 30 /usr/bin/bash -c 'until ip -4 addr show virbr0 2>/dev/null | grep -q " 192.168.122.1/"; do sleep 0.2; done'
